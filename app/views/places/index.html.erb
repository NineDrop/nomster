<br />
<div class="pagination justify-content-center">
<%= paginate @places, window: 2, outer_window: 2 %>
</div>

<% @places.each do |place| %>
	<div class="booyah-box col-10 offset-1">
		<h1><%=place.name %></h1>
		<i><%=place.address %></i><br /><br />
		<p><%=place.description %></p><br />
	</div>
<% end %> 

<div class="pagination justify-content-center">
<%= paginate @places, window: 2, outer_window: 2 %>
</div>